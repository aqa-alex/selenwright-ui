name: build

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  selenwright-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Build
        run: bash ci/build.sh

      - name: Latest image
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: bash ci/docker-push.sh latest

      - name: Latest docs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash ci/docs.sh latest ${{ secrets.GITHUB_TOKEN }}
